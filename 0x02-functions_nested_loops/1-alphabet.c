#include <stdio.h>
/**
 * main - prints the alphabet, in lowercase
 *
 * Return Always 0
 */
void print_alphabet(void)
{
	int alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; ch++)
		putchar(ch);
	putchar('\n')
	return (0);
}
